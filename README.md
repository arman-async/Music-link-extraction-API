##### ⚠️ Docker is still not working well

### Use for development
```git clone https://github.com/arman-async/Music-link-extraction-API.git```

```python -m pip install requirements.txt```

```python -m app```
